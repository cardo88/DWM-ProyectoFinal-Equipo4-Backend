// Rutas para producto
const express = require('express');
const router = express.Router();
const hangmanController = require('../controllers/hangmanController');

// api/productos
// router.post('/', hangmanController.createQuestion);
// router.get('/', hangmanController.getQuestions);
// router.put('/:id', hangmanController.updateQuestion);
// router.get('/:id', hangmanController.getQuestion);
// router.delete('/:id', hangmanController.deleteQuestion);

module.exports = router;